export const sendOtpToPhone = async (phoneNo, otp) => {
    console.log(`Sending OTP ${otp} to phone number ${phoneNo}`);
    // In a real implementation, you would integrate with an SMS service here.
};